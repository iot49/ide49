image: linuxserver/plex:latest
restart: unless-stopped
privileged: true
network_mode: host
volumes:
    - plex:/config
    - media:/media
environment:
    - PUID=1000
    - PGID=100
    - PLEX_CLAIM=claim-jifZ9RJFtLREjLDufCLn
    - VERSION=docker
http_port: 32400
